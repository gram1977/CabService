# Backend .env file
# This file is used by your Node.js/Express backend. It can contain sensitive information 
# (like database URIs, secrets, etc.) and is loaded at runtime using libraries like dotenv.
# libraries like dotenv read the .env file and load its key-value pairs into process.env
MONGODB_URI=mongodb+srv://amangreval_db_user:india123@amandb.mumly7r.mongodb.net/cabservice?retryWrites=true
MONGODB_DB=cabservice
PORT=3000